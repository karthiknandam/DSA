#include <bits/std++.h>
using namespace std;

void Dequeue(){ 
    deque <int> dq;
    dq.push_back(1);
    dq.push_front(8);
    dq.back();
    dq.front();

    // Rest is same as the vector;
    

}