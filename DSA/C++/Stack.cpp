#include <bits/std++.h>

using namespace std;

// LIFO Last in first out 
void LearnStack(){ 
    stack <int> s;

    s.push(2);

    s.pop();
    s.push(3);

    s.top();

    s.size();

    // s.swap
    s.empty();


}