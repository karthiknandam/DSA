#include <bits/std++.h>
using namespace std;

void Learnqueue(){ 

    queue<int> q;

    q.push(1);
    q.pop();

    q.size();
    q.back();

    cout << q.empty();

}

int main(){ 
    Learnqueue();
}